// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"juice/public/ent/predicate"
	"juice/public/ent/userbaseinfo"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// UserBaseInfoQuery is the builder for querying UserBaseInfo entities.
type UserBaseInfoQuery struct {
	config
	ctx        *QueryContext
	order      []userbaseinfo.OrderOption
	inters     []Interceptor
	predicates []predicate.UserBaseInfo
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the UserBaseInfoQuery builder.
func (ubiq *UserBaseInfoQuery) Where(ps ...predicate.UserBaseInfo) *UserBaseInfoQuery {
	ubiq.predicates = append(ubiq.predicates, ps...)
	return ubiq
}

// Limit the number of records to be returned by this query.
func (ubiq *UserBaseInfoQuery) Limit(limit int) *UserBaseInfoQuery {
	ubiq.ctx.Limit = &limit
	return ubiq
}

// Offset to start from.
func (ubiq *UserBaseInfoQuery) Offset(offset int) *UserBaseInfoQuery {
	ubiq.ctx.Offset = &offset
	return ubiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ubiq *UserBaseInfoQuery) Unique(unique bool) *UserBaseInfoQuery {
	ubiq.ctx.Unique = &unique
	return ubiq
}

// Order specifies how the records should be ordered.
func (ubiq *UserBaseInfoQuery) Order(o ...userbaseinfo.OrderOption) *UserBaseInfoQuery {
	ubiq.order = append(ubiq.order, o...)
	return ubiq
}

// First returns the first UserBaseInfo entity from the query.
// Returns a *NotFoundError when no UserBaseInfo was found.
func (ubiq *UserBaseInfoQuery) First(ctx context.Context) (*UserBaseInfo, error) {
	nodes, err := ubiq.Limit(1).All(setContextOp(ctx, ubiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{userbaseinfo.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) FirstX(ctx context.Context) *UserBaseInfo {
	node, err := ubiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first UserBaseInfo ID from the query.
// Returns a *NotFoundError when no UserBaseInfo ID was found.
func (ubiq *UserBaseInfoQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ubiq.Limit(1).IDs(setContextOp(ctx, ubiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{userbaseinfo.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) FirstIDX(ctx context.Context) int {
	id, err := ubiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single UserBaseInfo entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one UserBaseInfo entity is found.
// Returns a *NotFoundError when no UserBaseInfo entities are found.
func (ubiq *UserBaseInfoQuery) Only(ctx context.Context) (*UserBaseInfo, error) {
	nodes, err := ubiq.Limit(2).All(setContextOp(ctx, ubiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{userbaseinfo.Label}
	default:
		return nil, &NotSingularError{userbaseinfo.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) OnlyX(ctx context.Context) *UserBaseInfo {
	node, err := ubiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only UserBaseInfo ID in the query.
// Returns a *NotSingularError when more than one UserBaseInfo ID is found.
// Returns a *NotFoundError when no entities are found.
func (ubiq *UserBaseInfoQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = ubiq.Limit(2).IDs(setContextOp(ctx, ubiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{userbaseinfo.Label}
	default:
		err = &NotSingularError{userbaseinfo.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) OnlyIDX(ctx context.Context) int {
	id, err := ubiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of UserBaseInfos.
func (ubiq *UserBaseInfoQuery) All(ctx context.Context) ([]*UserBaseInfo, error) {
	ctx = setContextOp(ctx, ubiq.ctx, "All")
	if err := ubiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*UserBaseInfo, *UserBaseInfoQuery]()
	return withInterceptors[[]*UserBaseInfo](ctx, ubiq, qr, ubiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) AllX(ctx context.Context) []*UserBaseInfo {
	nodes, err := ubiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of UserBaseInfo IDs.
func (ubiq *UserBaseInfoQuery) IDs(ctx context.Context) (ids []int, err error) {
	if ubiq.ctx.Unique == nil && ubiq.path != nil {
		ubiq.Unique(true)
	}
	ctx = setContextOp(ctx, ubiq.ctx, "IDs")
	if err = ubiq.Select(userbaseinfo.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) IDsX(ctx context.Context) []int {
	ids, err := ubiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ubiq *UserBaseInfoQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ubiq.ctx, "Count")
	if err := ubiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ubiq, querierCount[*UserBaseInfoQuery](), ubiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) CountX(ctx context.Context) int {
	count, err := ubiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ubiq *UserBaseInfoQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ubiq.ctx, "Exist")
	switch _, err := ubiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ubiq *UserBaseInfoQuery) ExistX(ctx context.Context) bool {
	exist, err := ubiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the UserBaseInfoQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ubiq *UserBaseInfoQuery) Clone() *UserBaseInfoQuery {
	if ubiq == nil {
		return nil
	}
	return &UserBaseInfoQuery{
		config:     ubiq.config,
		ctx:        ubiq.ctx.Clone(),
		order:      append([]userbaseinfo.OrderOption{}, ubiq.order...),
		inters:     append([]Interceptor{}, ubiq.inters...),
		predicates: append([]predicate.UserBaseInfo{}, ubiq.predicates...),
		// clone intermediate query.
		sql:  ubiq.sql.Clone(),
		path: ubiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uint64 `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.UserBaseInfo.Query().
//		GroupBy(userbaseinfo.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ubiq *UserBaseInfoQuery) GroupBy(field string, fields ...string) *UserBaseInfoGroupBy {
	ubiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &UserBaseInfoGroupBy{build: ubiq}
	grbuild.flds = &ubiq.ctx.Fields
	grbuild.label = userbaseinfo.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uint64 `json:"user_id,omitempty"`
//	}
//
//	client.UserBaseInfo.Query().
//		Select(userbaseinfo.FieldUserID).
//		Scan(ctx, &v)
func (ubiq *UserBaseInfoQuery) Select(fields ...string) *UserBaseInfoSelect {
	ubiq.ctx.Fields = append(ubiq.ctx.Fields, fields...)
	sbuild := &UserBaseInfoSelect{UserBaseInfoQuery: ubiq}
	sbuild.label = userbaseinfo.Label
	sbuild.flds, sbuild.scan = &ubiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a UserBaseInfoSelect configured with the given aggregations.
func (ubiq *UserBaseInfoQuery) Aggregate(fns ...AggregateFunc) *UserBaseInfoSelect {
	return ubiq.Select().Aggregate(fns...)
}

func (ubiq *UserBaseInfoQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ubiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ubiq); err != nil {
				return err
			}
		}
	}
	for _, f := range ubiq.ctx.Fields {
		if !userbaseinfo.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ubiq.path != nil {
		prev, err := ubiq.path(ctx)
		if err != nil {
			return err
		}
		ubiq.sql = prev
	}
	return nil
}

func (ubiq *UserBaseInfoQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*UserBaseInfo, error) {
	var (
		nodes = []*UserBaseInfo{}
		_spec = ubiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*UserBaseInfo).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &UserBaseInfo{config: ubiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ubiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ubiq *UserBaseInfoQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ubiq.querySpec()
	_spec.Node.Columns = ubiq.ctx.Fields
	if len(ubiq.ctx.Fields) > 0 {
		_spec.Unique = ubiq.ctx.Unique != nil && *ubiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ubiq.driver, _spec)
}

func (ubiq *UserBaseInfoQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(userbaseinfo.Table, userbaseinfo.Columns, sqlgraph.NewFieldSpec(userbaseinfo.FieldID, field.TypeInt))
	_spec.From = ubiq.sql
	if unique := ubiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ubiq.path != nil {
		_spec.Unique = true
	}
	if fields := ubiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, userbaseinfo.FieldID)
		for i := range fields {
			if fields[i] != userbaseinfo.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ubiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ubiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ubiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ubiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ubiq *UserBaseInfoQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ubiq.driver.Dialect())
	t1 := builder.Table(userbaseinfo.Table)
	columns := ubiq.ctx.Fields
	if len(columns) == 0 {
		columns = userbaseinfo.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ubiq.sql != nil {
		selector = ubiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ubiq.ctx.Unique != nil && *ubiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ubiq.predicates {
		p(selector)
	}
	for _, p := range ubiq.order {
		p(selector)
	}
	if offset := ubiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ubiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// UserBaseInfoGroupBy is the group-by builder for UserBaseInfo entities.
type UserBaseInfoGroupBy struct {
	selector
	build *UserBaseInfoQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (ubigb *UserBaseInfoGroupBy) Aggregate(fns ...AggregateFunc) *UserBaseInfoGroupBy {
	ubigb.fns = append(ubigb.fns, fns...)
	return ubigb
}

// Scan applies the selector query and scans the result into the given value.
func (ubigb *UserBaseInfoGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ubigb.build.ctx, "GroupBy")
	if err := ubigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserBaseInfoQuery, *UserBaseInfoGroupBy](ctx, ubigb.build, ubigb, ubigb.build.inters, v)
}

func (ubigb *UserBaseInfoGroupBy) sqlScan(ctx context.Context, root *UserBaseInfoQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(ubigb.fns))
	for _, fn := range ubigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*ubigb.flds)+len(ubigb.fns))
		for _, f := range *ubigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*ubigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ubigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// UserBaseInfoSelect is the builder for selecting fields of UserBaseInfo entities.
type UserBaseInfoSelect struct {
	*UserBaseInfoQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ubis *UserBaseInfoSelect) Aggregate(fns ...AggregateFunc) *UserBaseInfoSelect {
	ubis.fns = append(ubis.fns, fns...)
	return ubis
}

// Scan applies the selector query and scans the result into the given value.
func (ubis *UserBaseInfoSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ubis.ctx, "Select")
	if err := ubis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*UserBaseInfoQuery, *UserBaseInfoSelect](ctx, ubis.UserBaseInfoQuery, ubis, ubis.inters, v)
}

func (ubis *UserBaseInfoSelect) sqlScan(ctx context.Context, root *UserBaseInfoQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ubis.fns))
	for _, fn := range ubis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ubis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ubis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
